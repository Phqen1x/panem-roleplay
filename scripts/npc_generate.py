#!/usr/bin/env python3
"""Generate level-0 NPC content from name/trait pools (Plan §6.1). Not
implemented yet: lands with Phase 3 (`data/npcs/*.yaml`, Spec §5.4)."""

from __future__ import annotations

if __name__ == "__main__":
    raise SystemExit(
        "scripts/npc_generate.py is a Phase 3 deliverable (NPC content schema "
        "and name/trait pools aren't authored yet)."
    )
